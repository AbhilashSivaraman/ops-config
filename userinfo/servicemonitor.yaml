apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: userinfo-monitor
  namespace: rony-hms
spec:
  selector:
    matchLabels:
      app: userinfo
  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s